---
phase: 01-3d-foundation-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/gallery/page.tsx
  - src/components/features/gallery/GalleryClient.tsx
  - src/components/features/gallery/GalleryScene.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /gallery renders a 3D canvas in the browser without SSR errors"
    - "Navigating to any non-gallery page loads zero Three.js code"
    - "npm run build succeeds with no Three.js SSR import errors"
  artifacts:
    - path: "src/app/gallery/page.tsx"
      provides: "Server Component entry point for gallery route"
      contains: "import GalleryClient"
    - path: "src/components/features/gallery/GalleryClient.tsx"
      provides: "Client Component with dynamic import ssr:false"
      contains: "ssr: false"
    - path: "src/components/features/gallery/GalleryScene.tsx"
      provides: "R3F Canvas with placeholder scene"
      contains: "Canvas"
  key_links:
    - from: "src/app/gallery/page.tsx"
      to: "src/components/features/gallery/GalleryClient.tsx"
      via: "standard import (Server Component imports Client Component)"
      pattern: "import.*GalleryClient"
    - from: "src/components/features/gallery/GalleryClient.tsx"
      to: "src/components/features/gallery/GalleryScene.tsx"
      via: "next/dynamic with ssr:false"
      pattern: "dynamic.*import.*GalleryScene.*ssr.*false"
---

<objective>
Install React Three Fiber stack and establish the SSR-safe dynamic import pipeline for the /gallery route.

Purpose: This is the foundation that all subsequent 3D work builds on. Getting the SSR boundary correct and verifying code splitting prevents cascading build failures in later plans.
Output: A /gallery route that renders an R3F Canvas with a colored box (proof of life), fully code-split from other routes.
</objective>

<execution_context>
@/Users/rjl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rjl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-3d-foundation-navigation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install R3F dependencies and verify build</name>
  <files>package.json</files>
  <action>
Run the following install command:
```
npm install three@0.170.0 @react-three/fiber@8.18.0 @react-three/drei@9.122.0
npm install -D @types/three@0.170.0
```

After install, run `npm run build` to verify the existing site still builds cleanly with the new dependencies in node_modules. No code changes yet -- just confirm the deps install without peer dep conflicts.

If drei@9.122.0 has peer dep warnings about R3F version, try drei@9.115.0 instead. The research noted this as a fallback.

If `npm run build` fails with Turbopack issues, that is a separate concern -- the build command uses webpack for production. Just verify production build works.
  </action>
  <verify>
- `npm ls three` shows 0.170.0
- `npm ls @react-three/fiber` shows 8.18.0
- `npm ls @react-three/drei` shows 9.x
- `npm run build` succeeds (exit code 0)
  </verify>
  <done>All four 3D packages installed, existing site builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create SSR-safe gallery route with proof-of-life Canvas</name>
  <files>
    src/app/gallery/page.tsx
    src/components/features/gallery/GalleryClient.tsx
    src/components/features/gallery/GalleryScene.tsx
  </files>
  <action>
Create three files following the two-layer SSR boundary pattern from the research:

**src/app/gallery/page.tsx** (Server Component -- NO 'use client'):
- Import and render GalleryClient
- Add metadata export: `title: "Virtual Gallery | America's Tapestry"`, `description: "Walk through Gallery 7..."`
- This file must NOT import anything from three, @react-three/fiber, or @react-three/drei

**src/components/features/gallery/GalleryClient.tsx** ('use client'):
- Mark with `'use client'` at top
- Use `next/dynamic` to import GalleryScene with `{ ssr: false }`
- Include a loading fallback: a full-height flex container with "Loading gallery..." text, centered, using Tailwind classes
- Render the dynamic component inside a div with `className="h-screen w-screen"` to give the Canvas full viewport

**src/components/features/gallery/GalleryScene.tsx** (default export, no 'use client' needed since it's dynamically imported by a client component):
- Import Canvas from @react-three/fiber
- Render a Canvas with:
  - A simple `<ambientLight intensity={0.5} />`
  - A colored box mesh at origin: `<mesh><boxGeometry args={[1,1,1]} /><meshStandardMaterial color="orange" /></mesh>`
  - Camera prop: `camera={{ fov: 75, position: [0, 2, 5] }}`
- This is a proof-of-life scene that will be replaced in Plan 02

After creating files, run:
1. `npm run build` -- must succeed
2. `npm run dev` and verify /gallery loads the orange box in browser (or verify build output shows the gallery route)

To verify code splitting, check the build output: the /gallery route should show Three.js chunks, but other routes (/, /tapestries, etc.) should NOT include Three.js in their chunks.
  </action>
  <verify>
- `npm run build` succeeds
- Build output shows /gallery as a route
- No Three.js import errors during build
- The file `src/app/gallery/page.tsx` does NOT contain 'use client'
- The file `src/components/features/gallery/GalleryClient.tsx` contains both 'use client' and 'ssr: false'
- `grep -r "from 'three'" src/app/` returns ONLY files under src/app/gallery/ (no leakage)
- `grep -r "from '@react-three" src/app/` returns ONLY files under src/app/gallery/ (no leakage)
  </verify>
  <done>
- /gallery route exists and renders an R3F Canvas with a visible 3D object
- Production build succeeds with zero SSR errors
- Three.js code is isolated to gallery route only (other pages load zero Three.js)
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes successfully
2. Build output shows /gallery route without SSR errors
3. No Three.js imports exist outside the gallery component tree
4. The two-layer SSR boundary is correct: Server Component page -> 'use client' wrapper -> dynamic(ssr:false) -> Canvas
</verification>

<success_criteria>
- All R3F dependencies installed at correct versions
- /gallery route renders a 3D canvas (proof of life)
- Production build succeeds
- Three.js is code-split -- zero Three.js code loaded on non-gallery pages
- SSR boundary follows the two-layer pattern from research
</success_criteria>

<output>
After completion, create `.planning/phases/01-3d-foundation-navigation/01-01-SUMMARY.md`
</output>
